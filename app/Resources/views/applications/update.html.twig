{% extends 'base.html.twig' %}

{% block body %}
	<div class="d-flex align-items-center justify-content-between">
        <h1 class="display-4"><span class="fas fa-pencil-alt"></span> {{ application.username }}</h1>
        <a href="{{ path('applications') }}" class="btn btn-secondary">
            <span class="fas fa-list"></span> <span class="hidden-xs">Retour à la liste</span>
        </a>
    </div>
	<hr/>
	{% include 'applications/form.html.twig' %}
{% endblock %}