<div class="card">
  	<div class="card-header">
    	Informations
  	</div>
  	<div class="card-body">
  		{{ form_start(form) }}    
		    <!-- NAME -->
		    {{ form_row(form.name) }}

		    <button type="submit" class="btn btn-primary">
		    	<span class="fas fa-save"></span> Enregistrer
			</button>
		{{ form_end(form) }}
  	</div>
</div>